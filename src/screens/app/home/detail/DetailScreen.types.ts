import {RouteProp} from '@react-navigation/native';
import {HomeStackRouter, HomeStackRoutes} from '../../../../routes';

export type DetailScreenNavigationProp = RouteProp<
  HomeStackRouter,
  HomeStackRoutes.DETAIL
>;
